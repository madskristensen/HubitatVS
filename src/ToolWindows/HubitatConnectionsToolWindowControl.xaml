<UserControl x:Class="HubitatVS.HubitatConnectionsToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit"
             xmlns:platformUI="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
             xmlns:windows="clr-namespace:System.Windows;assembly=PresentationFramework"
             toolkit:Themes.UseVsTheme="True"
             MinHeight="120">
    <Grid Margin="8,4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" MinWidth="180"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="3*" MinWidth="220"/>
        </Grid.ColumnDefinitions>

        <!-- Left: hub list + buttons -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DataGrid x:Name="HubsGrid"
                      Grid.Row="0"
                      ItemsSource="{Binding Hubs}"
                      IsReadOnly="True"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Background="{DynamicResource {x:Static platformUI:EnvironmentColors.ToolWindowBackgroundBrushKey}}"
                      RowBackground="{DynamicResource {x:Static platformUI:EnvironmentColors.ToolWindowBackgroundBrushKey}}"
                      Foreground="{DynamicResource {x:Static platformUI:EnvironmentColors.ToolWindowTextBrushKey}}"
                      BorderBrush="{DynamicResource {x:Static platformUI:EnvironmentColors.ToolWindowBorderBrushKey}}"
                      SelectionChanged="HubsGrid_SelectionChanged">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource {x:Static platformUI:EnvironmentColors.ToolWindowBackgroundBrushKey}}"/>
                        <Setter Property="Foreground" Value="{DynamicResource {x:Static platformUI:EnvironmentColors.ToolWindowTextBrushKey}}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static platformUI:EnvironmentColors.ToolWindowBorderBrushKey}}"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="Padding" Value="4,3"/>
                        <Setter Property="SeparatorBrush" Value="{DynamicResource {x:Static platformUI:EnvironmentColors.ToolWindowBorderBrushKey}}"/>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="{DynamicResource {x:Static platformUI:EnvironmentColors.ToolWindowBackgroundBrushKey}}"/>
                        <Setter Property="Foreground" Value="{DynamicResource {x:Static platformUI:EnvironmentColors.ToolWindowTextBrushKey}}"/>
                        <Style.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="primitives:Selector.IsSelectionActive" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" Value="{DynamicResource {x:Static platformUI:EnvironmentColors.SystemHighlightBrushKey}}"/>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static platformUI:EnvironmentColors.SystemHighlightTextBrushKey}}"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="primitives:Selector.IsSelectionActive" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" Value="{DynamicResource {x:Static windows:SystemColors.InactiveSelectionHighlightBrushKey}}"/>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static platformUI:EnvironmentColors.ToolWindowTextBrushKey}}"/>
                            </MultiTrigger>
                        </Style.Triggers>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Background}"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Foreground}"/>
                    </Style>
                    <Style x:Key="GridTextStyle" TargetType="TextBlock">
                        <Setter Property="Foreground" Value="{DynamicResource {x:Static platformUI:EnvironmentColors.ToolWindowTextBrushKey}}"/>
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True"/>
                                    <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=(primitives:Selector.IsSelectionActive)}" Value="True"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static platformUI:EnvironmentColors.SystemHighlightTextBrushKey}}"/>
                            </MultiDataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True"/>
                                    <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=(primitives:Selector.IsSelectionActive)}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static platformUI:EnvironmentColors.ToolWindowTextBrushKey}}"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" ElementStyle="{StaticResource GridTextStyle}"/>
                    <DataGridTextColumn Header="Host" Binding="{Binding Host}" Width="*" ElementStyle="{StaticResource GridTextStyle}"/>
                </DataGrid.Columns>
            </DataGrid>

            <WrapPanel Grid.Row="1" Margin="0,4,0,2">
                <Button Content="Test"   Width="50" Margin="0,0,4,0" Click="TestConnection_Click"/>
                <Button Content="New"    Width="50" Margin="0,0,4,0" Click="NewHub_Click"/>
                <Button Content="Delete" Width="55"                   Click="DeleteHub_Click"/>
            </WrapPanel>

            <TextBlock x:Name="StatusText" Grid.Row="2" Margin="0,2,0,0"
                       TextWrapping="Wrap" FontSize="11"/>
        </Grid>

        <!-- Vertical splitter -->
        <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch"
                      Background="{DynamicResource {x:Static platformUI:EnvironmentColors.ToolWindowBorderBrushKey}}"/>

        <!-- Right: edit form -->
        <Grid Grid.Column="2" Margin="8,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Name:"     VerticalAlignment="Center" Margin="0,3"/>
            <TextBox x:Name="HubNameBox"     Grid.Column="1" Margin="4,2"/>

            <TextBlock Grid.Row="1" Text="Host:"     VerticalAlignment="Center" Margin="0,3"/>
            <TextBox x:Name="HubHostBox"     Grid.Row="1" Grid.Column="1" Margin="4,2"/>

            <TextBlock Grid.Row="2" Text="Username:" VerticalAlignment="Center" Margin="0,3"/>
            <TextBox x:Name="HubUsernameBox" Grid.Row="2" Grid.Column="1" Margin="4,2"/>

            <TextBlock Grid.Row="3" Text="Password:" VerticalAlignment="Center" Margin="0,3"/>
            <PasswordBox x:Name="HubPasswordBox" Grid.Row="3" Grid.Column="1" Margin="4,2"
                         PasswordChanged="HubPasswordBox_PasswordChanged"/>

            <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="4,6,0,0">
                <Button Content="Save"  Width="60" Margin="0,0,6,0" Click="SaveHub_Click"/>
                <Button Content="Clear" Width="60"                   Click="ClearForm_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
